JWT stands for JSON Web Token and is a common [[Security]] option for web pages.
JWT are split into three parts Header, Payload and Signature
JWT scales well, best for microservices
But they are tard to revoke and the payload is visible to the user.
![[Pasted image 20260623111507.png]]