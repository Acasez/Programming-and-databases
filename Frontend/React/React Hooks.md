Hooks are built in functions in [[React]] that can be used to do different things

The three common hooks are these 
- useRef
- useState
- useEffect
These two are sometimes used
- useMemo
- useCallbacl
There are other built in hooks, but they are rarer
[Built-in React Hooks – React](https://react.dev/reference/react/hooks "https://react.dev/reference/react/hooks")

Its also possible to create custom hooks